/*
  Header of the project.
  Displays the signboard at the top.
*/

import React from "react";
import "../css/App.css"

const Header = () => {
  return (
    <div className="header">
      <span className="header-title">Hangman</span>
    </div>
  );
};

export default Header;
